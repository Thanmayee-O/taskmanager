# Focus Frontend

Vite-based React SPA for Focus Personal Task Manager. Built using Tailwind CSS for UI styling.

## Running the Web App

1. Ensure the backend API is running on `http://localhost:5000`
2. Install dependencies: `npm install`
3. Run dev server: `npm run dev` (Runs on Port 5173)
